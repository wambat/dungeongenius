(ns dg.client.dungeon.layout
  (:require [taoensso.timbre :as timbre
             :refer-macros [log
                            trace
                            debug
                            info
                            warn
                            error
                            fatal
                            report]]))

(defn main [params]
  (str params))

